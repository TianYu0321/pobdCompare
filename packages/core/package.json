{
  "name": "@pobd/core",
  "version": "0.1.0",
  "description": "核心引擎：Baseline、Variant、Mutation、Comparator、Analyzer、JobQueue",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@pobd/schemas": "0.1.0",
    "@pobd/pob2-worker": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.12.0"
  }
}